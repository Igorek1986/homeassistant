    # # Сенсор автоотключения света 
      # - platform: template
        # sensors:
          # auto_off_svet_marina_zavod:
            # friendly_name: "Автоотключение света Марина"
            # value_template: >-
              # {{ is_state('switch.zavod_0x158d0003222bf3_switch', 'on')  
                 # and is_state('binary_sensor.zavod_0x158d000394082b_motion', 'off')  }}
            # icon_template: >-
              # {% if is_state("binary_sensor.auto_off_svet_marina_zavod", "on") %}
              # mdi:timer
              # {% else %}
              # mdi:timer-off
              # {% endif %}