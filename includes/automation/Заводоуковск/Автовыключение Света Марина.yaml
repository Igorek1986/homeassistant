# #Выключение света Марина
# - alias: motion_zal_svet_off
  # initial_state: true
  # trigger:
# # Датчик движения подключенный через zigbe2mqtt
  # - platform: state
    # entity_id: binary_sensor.auto_off_svet_marina_zavod
    # to: 'on'
    # for: "0:{{ states('input_number.auto_off_svet_marina_zavod') |int }}"
    # # for:
     # # minutes: 30
  # condition:
    # - condition: state
      # entity_id: switch.zavod_0x158d0003222bf3_switch
      # state: 'on'
  # action:
  # - service: switch.turn_off
    # entity_id: switch.zavod_0x158d0003222bf3_switch